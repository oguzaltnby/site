[build]
  functions = "netlify/functions"
  publish = "dist"
  node_version = "18.17.0"  # Node.js sürümünü güncelleyin
  command = "npm install --omit=optional && nuxt generate"

[dev]
  command = "npm run dev"
  port = 3000

[functions]
  included_files = ["netlify/functions/files/**"]
